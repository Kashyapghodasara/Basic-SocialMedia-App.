<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-800">
    <h1 class="text-4xl text-zinc-200 mt-6 mb-10 ml-10">Edit Your Post...✍</h1>

    <form action="/edit/<%= post._id %>" method="post" class="block">
        <textarea placeholder="Update Your Post..." required name="content" class="resize-none bg-transparent bg-zinc-700 ml-10 mt-5 p-2 rounded-md border-[1px] w-1/2 h-20 text-white"><%= post.content %></textarea>
        <div class="flex ml-10 mt-2 gap-4">
            <input type="submit" value="Update Post" class="p-2 bg-yellow-500 w-36 font-semibold text-md text-white rounded-md">
            <a href="/profile" class="p-2 bg-blue-600 w-36 font-semibold text-md text-white rounded-md text-center">Profile</a>
        </div>
    </form>
</body>
</html>